/**
 * Feature flags configuration
 * Toggle features on/off for the application
 */
export const featureFlags = {
  /**
   * Enable/disable the "Process Queries" button
   * Set to false to hide the button
   */
  showProcessQueriesButton: false,
  /**
   * Enable/disable the chat history panel UI
   * Keyboard history navigation remains enabled regardless
   */
  enableChatHistoryPanel: true
};

